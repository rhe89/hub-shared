@inherits ButtonBase

@inject State _state

<button
    class="btn btn-primary @Class"
    disabled="@(_state.Working || @SetDisabled)"
    @attributes="AdditionalAttributes">
    @ChildContent
</button>